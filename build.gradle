allprojects {
    apply plugin: 'java'
}

task wrapper(type: Wrapper) {
    gradleVersion = '4.6'
}

project(':core') {
    jar {
       baseName = 'zip4j'
        version =  '1.3.2'
    }
}

project(':examples') {
   dependencies {
        compile project(':core')
    }
}
